# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Tunes with Friends' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Tunes with Friends
  pod 'Parse'
  pod 'Parse/UI'

  target 'Tunes with FriendsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Tunes with FriendsUITests' do
    # Pods for testing
  end

end
